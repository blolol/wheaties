#!/usr/bin/env ruby

require 'bundler'
Bundler.require(:default, :commands)

Dotenv.load

$:.unshift File.join(File.dirname(__FILE__), '../lib')
require 'wheaties'

require 'pry'
binding.pry(quiet: true)
