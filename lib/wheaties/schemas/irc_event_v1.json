{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "$id": "https://blolol.com/json-schemas/irc_event_v1.json",
  "type": "object",
  "properties": {
    "version": {
      "enum": [1, "1"]
    },
    "event": {
      "type": "object",
      "properties": {
        "type": {
          "enum": ["action", "message", "notice", null]
        },
        "from": {
          "$comment": "Valid RELAYMSG nickname containing a forward slash",
          "type": ["string", "null"],
          "pattern": "^\\w+\\/\\w+$"
        },
        "to": {
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": ["to", "text"]
    }
  },
  "required": ["event"]
}
